# Web media is deliberately tracked as ordinary Git blobs, not LFS pointers.
# Each published file remains below 25 MB; original masters stay in the archive.
public/media/** -text -filter -diff -merge
